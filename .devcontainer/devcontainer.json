{
  "image": "mcr.microsoft.com/devcontainers/python:3.13",
  "postCreateCommand": "python -m pip install -U pip && pip install ipykernel && pip install -e . && python -m ipykernel install --user --name=py313-demo --display-name='Python 3.13 (demo)'",

  "customizations": {
    "vscode": {
      "extensions": ["ms-toolsai.jupyter", "ms-python.python"],
      "settings": {
        "workbench.startupEditor": "none",
        "jupyter.alwaysTrustNotebooks": true,
        "python.defaultInterpreterPath": "/usr/local/bin/python3.13"
      }
    },
    "codespaces": {
      "openFiles": ["notebooks/Quickstart.ipynb"]
    }
  },
  "postAttachCommand": "bash -lc 'if [ -f notebooks/Quickstart.ipynb ]; then code -r notebooks/Quickstart.ipynb; fi'"
}
